# We need to define some constants such that they can be used in multiple files such as config.yaml path
from pathlib import Path

CONFIG_FILE_PATH =  Path("config/config.yaml")
PARAMS_FILE_PATH = Path("params.yaml")
